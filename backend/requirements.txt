# Deployment dependencies. Keep in sync with [project.dependencies] in pyproject.toml,
# which remains the source of truth for local development via uv.
alembic==1.16.5
fastapi==0.116.1
olefile==0.47
psycopg2-binary==2.9.10
pydantic==2.11.7
pypdf==6.1.0
python-dotenv==1.1.1
python-multipart==0.0.20
sqlalchemy==2.0.43
uvicorn[standard]==0.35.0
